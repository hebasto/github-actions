name: CI
on: push

jobs:
  ctest-exception:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          cmake -B build
          cmake --build build
          ctest --test-dir build --output-on-failure
