name: CI
on: push

jobs:
  check-os:
    runs-on: macos-14
    steps:
      - name: ${{ runner.arch }}
        run: uname -m
      - run: ls -la /Applications
      - run: printenv | grep -i arm || true
      - run: printenv | grep -i aarch || true
