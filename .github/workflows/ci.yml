name: CI
on: push

jobs:
  check-os:
    name: "Hello macOS"
    runs-on: macos-13
    steps:
      - run: uname -m
      - run: printenv | grep RUNNER_ARCH

      - run: |
          brew install python@3.12

      - run: brew link --overwrite python@3.12 --dry-run
