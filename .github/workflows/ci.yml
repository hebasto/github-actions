on: push

jobs:
  valgrind:
    runs-on: macos-13
    steps:
      - run: |
          brew tap hebasto/valgrind
          brew install hebasto/valgrind/valgrind@230826-ventura-supp
          valgrind --version

      - run: |
          valgrind --gen-suppressions=all ls
