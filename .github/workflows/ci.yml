on: push

jobs:
  valgrind:
    runs-on: macos-13
    steps:
      - run: |
          brew tap hebasto/valgrind
          brew install --HEAD hebasto/valgrind/valgrind
          valgrind --version

      - run: |
          valgrind --gen-suppressions=all ls
