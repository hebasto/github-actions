name: CI
on: push

jobs:
  check-os:
    name: "Hello macOS"
    runs-on: macos-13
    steps:
      # - run: |
      #     brew uninstall --ignore-dependencies python python3 python@3 python@3.12

      # - run: |
      #     rm /usr/local/bin/2to3
      #     rm /usr/local/bin/2to3-3.12

      - run: |
          brew install python@3.12 || brew link --overwrite python@3.12
