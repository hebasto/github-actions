name: CI
on: push

jobs:
  check-os:
    name: "Hello macOS"
    runs-on: macos-13
    steps:
      - run: |
          brew uninstall --ignore-dependencies python python3 python@3 python@3.12

      - run: |
          brew install python@3.12
