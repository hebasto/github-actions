name: CI
on: push

jobs:
  NetBSD:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Start NetBSD VM
        uses: vmactions/netbsd-vm@v1
        with:
          sync: no

      - shell: netbsd {0}
        run: ulimit -a
